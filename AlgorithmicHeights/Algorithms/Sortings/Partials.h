#ifndef PARTIALS_H
#define PARTIALS_H

template<typename T>
void find_num_greatest(T* array, int size, int num){
    //build max-heap
    //extract %num elements
}


template<typename T>
void find_num_lowes(T* array, int size, int num){
    //build min-heap
    //extract %num elements
}

#endif // PARTIALS_H
